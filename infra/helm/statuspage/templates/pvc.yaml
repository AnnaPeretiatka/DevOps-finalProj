apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: statuspage-static
  namespace: statuspage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: gp3-csi